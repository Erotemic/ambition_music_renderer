# Music experiments

This directory is for generated or hand-authored score experiments that should
not replace `scores/active/*` until they are accepted. The transition-lab helper
writes `first_goblin_transition_lab.music.yaml` here.
